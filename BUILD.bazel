load("//rules:defs.bzl", "rules")

rules.derive(
    name = "split_ssl",
    targets = ["//example:ssl"],
    systems = [
        "x86_64-linux",   # build @gtest//:gtest --//flag:system=x86_64-linux
        "aarch64-linux",  # build @gtest//:gtest --//flag:system=aarch64-linux
    ],
)
